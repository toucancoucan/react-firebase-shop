import {reviewType} from "../Reducers/ShopReducer";

export type addReviewToProduct = (review: reviewType, productId: string) => void;

export let addReviewToProduct: addReviewToProduct = (review, productId) => {

}